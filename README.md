# TaskMaster-backend
TaskMaster Backend — the server-side component of the task management application. It implements authentication, CRUD operations for tasks and users, and provides an API for frontend integration.
